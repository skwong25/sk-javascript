/*  INTRO: Arrays
- One way to create an array is an array literal by wrapping items in []
- Arrays can store 'elements' of all data types
- Arrays can be saved to a Variable
- We can access or redefine elements via their 'index' (element's position) 
- JS arrays are Zero-Indexed (position count starts from 0)*/

let cities = ['NY','HK','LA']
console.log (cities [0]) // Prints 'NY' 

let NY = 'New York'
console.log (NY[6])

cities [2] = 'DE'
console.log(cities) // Prints